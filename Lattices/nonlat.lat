; nonlat.lat
;;;  a structure version

(in-package :user)

(setq nonlat (make-finite-lattice
    ;; '(0 1 a b c d)
    '(
      (0 (a b ))
      (a (c d))
      (b (c d))
      (c (1))
      (d (1))
      (1 ())  ) ) )

